# Cmd+F to Focus Search Bar in Active Pane

**Overall Progress:** `100%`

## TLDR
Add Cmd+F shortcut to focus the search/find bar in whichever pane currently has focus. Terminal focuses its find bar, file explorer activates FAYT filter mode, preview already works.

## Critical Decisions
- **Use `performKeyEquivalent` pattern**: Matches existing Preview implementation — responder chain naturally routes to the focused pane's VC, no central routing needed
- **File explorer triggers `enterFilterMode()`**: Not path editing — Cmd+F opens the FAYT filter so the user can immediately start typing to filter the file list
- **No changes to RootSplitViewController**: Focus tracking already works correctly; each pane handles its own Cmd+F independently

## Tasks:

- [x] 🟩 **Step 1: Terminal — Add Cmd+F to focus find bar**
  - [x] 🟩 Add `performKeyEquivalent` override in `TerminalContainerViewController.swift`
  - [x] 🟩 On Cmd+F, call `findBar.focus()` and return `true`

- [x] 🟩 **Step 2: File Explorer — Add Cmd+F to activate FAYT filter**
  - [x] 🟩 Add `performKeyEquivalent` override in `FilePanelViewController.swift`
  - [x] 🟩 On Cmd+F, call `breadcrumbBar.enterFilterMode()` and return `true`

- [x] 🟩 **Step 3: Verify Preview (already implemented)**
  - [x] 🟩 Confirmed `PreviewContainerViewController.swift` Cmd+F works — same pattern used for terminal and file explorer
